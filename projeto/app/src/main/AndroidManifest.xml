<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.telasprojeto">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".TelaAtendimento3"></activity>
        <activity android:name=".TelaAtendimento2" />
        <activity android:name=".TelaAtendimento1" />
        <activity android:name=".Activity_usuario" />
        <!--<activity android:name=".TelaAtendimento" />-->
        <activity android:name=".ListaRemedios" />
        <activity android:name=".ListaPacientes" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar.Bridge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaMedicos" />
        <activity android:name=".TelaPrincipal" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>